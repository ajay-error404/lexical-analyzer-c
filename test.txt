#include <stdio.h>

int main() {
    // This is a single-line comment
    int count = 0; /* Multi-line
                      comment */
    float pi = 3.14;
    char grade = 'A';
    char* message = "Hello, Lexer!";

    if (count < 10) {
        printf("%s\n", message);
    }
    return 0;
}

